# Solana PDA Generator CLI

A __dead__-simple, CLI tool to generate solana PDAs from the terminal. Especially useful when rekeying programs.

## Usage

`solana-pda-cli "program_id" "seed1" "seed2" ... "seed16"`